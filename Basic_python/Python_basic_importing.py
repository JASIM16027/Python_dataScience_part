"""import converter
from converter import lbs_to_kg, kg_to_lbs

print(lbs_to_kg(76))
print(kg_to_lbs(76))
"""

"""
def square(number):
    return number * number


b = square(10)
c = square(20)
d = square(30)
print(b, c, d)



def emojis_conveter(message):
    words = message.split()
    emojis = {

        ":)": "😂",
        "(:": "😢"

    }

    output = ""
    for word in words:
        output += emojis.get(word, word) + " "
    return output


message = input(">")
print(emojis_conveter(message))


#while True:

    try:
            Age = int(input("Age : "))

            if 0 <= Age <= 150:

                income = 2000
                risk = income / Age

                print(Age)

            elif Age > 150:
                print('Age is too much')

            else:
                print('Age can not be negative')

    except ZeroDivisionError:
        print("Age can't be Zero")
    except ValueError:
        print('Invalid value. please type Integer value😊😊')
"""
